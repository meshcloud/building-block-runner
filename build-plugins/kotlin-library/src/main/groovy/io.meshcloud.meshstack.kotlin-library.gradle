/**
 The kotlin-library plugin sets up a standard build for a kotlin library.
 The dependencies specified here should only include the "must haves" that all our libraries depend on
*/

plugins {
  id('io.meshcloud.meshstack.kotlin')
  id('java-library')
}

dependencies {
}
