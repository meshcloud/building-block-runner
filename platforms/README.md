# Platforms

Platforms control transitive dependencies, see https://docs.gradle.org/6.2.1/userguide/platforms.html.
We are currently migrating to this approach from the previously used "cross project" configuration 
(`allProjects`/`subProjects` in main `build.gradle`).
